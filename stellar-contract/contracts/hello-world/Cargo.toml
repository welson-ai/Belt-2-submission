[package]
name = "belt2_hello_world"
version = "1.0.0"
edition = "2021"
description = "BELT-2 Stellar Soroban smart contract"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
